<div id="header">
  <div class="m-3" style="float: right;">
    @if (User.Identity.IsAuthenticated)
    {
      <p>Logged in: @User.Identity.Name</p>
      <p>Done? @Html.ActionLink("log out", "Index", "Account")</p>
    }
    else
    {
      <p>@Html.ActionLink("log in", "Login", "Account")</p>
      <p>@Html.ActionLink("register", "Register")</p>
    }
  </div>
  <a href="/">
    <img id="logo" src="~/img/logo.png" alt="logo for application, click on routes to home page">
  </a>

  <div id="navbar">
    <a class="navButton m-2">@Html.ActionLink("Treats", "Index", "Treats")</a>
    <a class="navButton m-2">@Html.ActionLink("Flavors", "Index", "Flavors")</a>
  </div>
  <hr />
</div>