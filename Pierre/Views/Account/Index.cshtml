@{
  ViewData["Title"] = "Account Index View";
  Layout = "_Layout";
}
<h2>Authentication Page</h2>
<hr />
@if (User.Identity.IsAuthenticated)
{
  <p>Hello @User.Identity.Name!</p>
  @using (Html.BeginForm("LogOff", "Account"))
  {
    <input type="submit" class="button-55" value=" log out" />
  }
}
else
{
  <p class="button-55">@Html.ActionLink("register", "Register")</p>
  <p class="button-55">@Html.ActionLink("log in", "Login")</p>
}