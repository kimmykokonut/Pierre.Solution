@{
  ViewData["Title"] = "Account Index View";
  Layout = "_Layout";
}
<h2>Authentication w identity</h2>
<hr />
@if (User.Identity.IsAuthenticated)
{
  <p>Hello @User.Identity.Name!</p>
}
else
{
  <p>@Html.ActionLink("register", "Register")</p>
  <p>@Html.ActionLink("log in", "Login")</p>
}
